{
    "name": "cookie-king-worker",
    "version": "0.1.0",
    "type": "module",
    "private": true,
    "devDependencies": {
        "wrangler": "^3.0.0"
    },
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "test": "node test/contract.test.mjs"
    }
}
